"""Àlááfìa geography spine.

Canonical Nigerian administrative hierarchy (national → state → LGA → ward →
settlement), stable P-codes, name aliases, a fuzzy name resolver, a point-in-
polygon resolver, and source→canonical crosswalks (DHS first).

Everything in the Àlááfìa platform keys to `pcode` + a period. This package is
what produces `pcode`.
"""

__version__ = "0.1.0"
